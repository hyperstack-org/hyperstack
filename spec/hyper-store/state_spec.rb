require 'spec_helper'

describe "state methods" do
  context "the state and mutate methods" do
    it "will rerender a component when an instance state is mutated"
    it "will rerender a component when a class state is mutated"
  end

  context "the state_reader method" do
    it "will define a class reader method by the same name as the state"
    it "will define a class reader method with a diffe"

  end

  context "the private_state method" do
  end

end
