require 'spec_helper'

describe Syncromesh do
  it 'has a version number' do
    expect(Syncromesh::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
